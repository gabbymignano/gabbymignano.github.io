Q1: interval centered around mean w 8 st dev guaranteed to include at least what % of data?

[ ]
# Chebyschev's Theorem:t = 4(1-1/(t*t))*100

93.75
Q2: False; scaling data by quantiles does not require zscore computation first

Q3: mtcars dataset, zscore of 18.1 mpg car is?

[ ]
import numpy as npimport pandas as pd

[ ]
data = pd.read_csv('drive/MyDrive/Data Sci/mtcars.csv')data.mpg.values[5]

18.1
[ ]
data

[ ]
mpg = data.mpg.valueszs = (mpg-np.mean(mpg))/np.std(mpg)zs[5]

-0.3355723336253064
[ ]
from sklearn import linear_modelfrom sklearn.preprocessing import StandardScaler, QuantileTransformerscaler = StandardScaler()scaler.fit(data[['mpg']])scaler.fit_transform(data[['mpg']])
